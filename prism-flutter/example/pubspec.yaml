name: prism_flutter_example
description: Example app demonstrating the Prism 3D engine in Flutter.

publish_to: 'none'

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: '>=3.16.0'

dependencies:
  flutter:
    sdk: flutter
  prism_flutter:
    path: ../flutter_plugin

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
